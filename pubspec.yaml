name: alergeni
description: "Pollen and Allergen data for Serbia"
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^8.2.2
  flutter_map_marker_cluster: ^8.2.2
  http: ^1.6.0
  latlong2: ^0.9.1
  provider: ^6.1.5+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
